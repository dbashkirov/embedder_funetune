[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "embedder-funetune"
version = "0.1.0"
description = "Library for question generation and fine-tuning embedding models"
authors = [
  { name = "AutoGenerated" }
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "chromadb>=0.4.24",
  "qdrant-client>=1.7.0",
  "openai>=1.12.0",
  "python-dotenv>=1.0.1",
  "transformers>=4.38.0",
  "datasets>=2.16.0",
  "torch>=2.1.0",
  "peft>=0.8.0",
  "wandb>=0.16.0"
]

[project.optional-dependencies]
dev = ["pytest"]
